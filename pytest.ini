[pytest]
testpaths = tests
# 프로젝트 루트를 import 경로에 넣는다.
# 없으면 `pytest`(실행파일)로 돌릴 때 CWD가 sys.path 에 없어
# `import src` / `import main` 이 실패한다(`python -m pytest` 로는 우연히 통과).
pythonpath = .
markers =
    smoke: 헤드리스 Chrome 기동 스모크(외부 사이트/구글 미접속). Chrome 필요.
filterwarnings =
    ignore::DeprecationWarning
