# CI/개발용 도구 (런타임 의존성은 requirements.txt)
#
# 버전을 '핀' 하는 이유: 범위 지정(>=)으로 두면 CI가 매번 최신 툴을 받아,
# 코드를 안 바꿔도 새 lint 규칙 때문에 빌드가 갑자기 깨진다(실제로 ruff
# 0.15 -> 0.16 에서 발생). 툴 업그레이드는 의도적으로 커밋해서 올린다.
-r requirements.txt
pytest==8.4.2
ruff==0.16.6
mypy==1.19.1
